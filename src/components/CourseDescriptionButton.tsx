type Props = {
    courseId: string;
}

export default function CourseDescriptionButton({
    courseId
}: Props) {
  return (
    <div>CourseDescriptionButton</div>  // ToDo: Implement CourseDescriptionButton
  )
}